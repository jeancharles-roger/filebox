#summary Projects milestones
#labels Phase-Design

= M1 (no date yet) =

 * Uses of an OSGI Architecture,
 * Basic UI using SWT,
 * Preferences save,
 * Services discovery over a local network (with zeroconfig network),
 * Simple network operations (including file transfer) through RMI.

= M2 (no date yet) =

*(to be defined)*